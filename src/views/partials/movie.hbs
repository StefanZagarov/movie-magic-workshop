<div class="flip flip-vertical">
    <div class="front">
        <img src="{{imageUrl}}" alt="{{title}}">
    </div>
    <div class="back">
        <h1>{{title}}</h1>
        <h2>Genre: {{genre}}</h2>
        <p>Description: {{description}}</p>
        {{!-- To access the specific movie's page, we need an identifier for that movie, so we gave each movie an id,
        which we will access through the URL address with id of the movie --}}
        <a href="/movies/{{_id}}/details" class="details">Details</a>
    </div>